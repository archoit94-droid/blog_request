# 인계 문서 — 프론트 담당자용

네이버 블로그 제작 요청 앱의 **프론트 작업 담당자**(2026-07-29부터 스티브)를 위한 문서.
GAS·구글시트·결제는 인계 대상이 아니며 토미가 계속 관리한다.

---

## 작업 범위

폼이 가격대별로 나뉘어 있다. 둘 다 라이브다.

| 폴더 | 상품 |
|---|---|
| `10만원-CRM판매/` | 기존 원장 전용 10만원. 서명 링크로만 접근 가능 |
| `15만원-여름특가/` | 여름특가 15만원. 일반 공개 + 랜딩 보유 |

| 할 수 있는 것 | 위치 |
|---|---|
| 신청폼 프론트 수정 (문구·이미지·레이아웃·CSS) | 각 폴더의 `index.html` / `form.html` |
| 랜딩페이지 수정 | `15만원-여름특가/landing/` |
| 상세페이지 신규 제작 | 새 폴더 생성 후 본인 Vercel 계정으로 배포 |

## 손대면 안 되는 것

아래 요청이 들어오면 **작업을 멈추고 토미에게 확인**한다. 그냥 진행하면 라이브가 깨지거나 돈이 잘못 나간다.

1. **GAS 코드** (`gas-code/`)
   → 배포 권한이 없어 고쳐도 반영되지 않는다. 폼별로 독립 GAS라 잘못 건드리면 한쪽만 죽는다.

2. **폼 입력 필드 추가·삭제·이름 변경**
   → 구글시트 열 위치와 GAS 기록 로직까지 연결돼 있다. 프론트만 바꾸면 데이터가 엉뚱한 열에 들어간다.

3. **금액·가격 변경**
   → 결제 관련 값이 GAS에 박혀 있다. 화면 가격만 바꾸면 **표기와 실제 결제액이 어긋난다.**

4. **구글시트 구조 변경** (탭 이름, 열 순서, 열 추가·삭제)
   → 코드가 탭 이름과 열 위치로 데이터를 찾는다. 시트에 보호가 걸려 있다.

5. **서명 링크 관련 값을 코드에 적는 것** (10만원 폼)
   → `LINK_SECRET` 같은 비밀키는 GAS 스크립트 속성에만 둔다. **이 repo는 public이라 코드에 적으면 그대로 공개된다.**

6. **`10만원-CRM판매/`의 `FORM_BASE_URL`**
   → 반드시 `…/blog_request/10만원-CRM판매/` 여야 한다. 루트로 바꾸면 15만원 폼으로 튕긴다.

## 배포

| 대상 | 방법 | 반영 시간 |
|---|---|---|
| 신청폼 | `git push origin main` | GitHub Pages 자동, 수 분 |
| 랜딩·상세페이지 | `npx vercel --prod` (본인 Vercel 계정) | 즉시 |

라이브:
- 루트(→15만원): https://gobangmkt.github.io/blog_request/
- 10만원 폼: https://gobangmkt.github.io/blog_request/10만원-CRM판매/

## 되돌리기

프론트 사고는 되돌릴 수 있다. 명령어 없이 웹에서 처리한다.

1. https://github.com/gobangMkt/blog_request/commits/main
2. 문제가 된 커밋 클릭
3. 우측 `···` → **`Revert`**

되돌린 것도 커밋으로 남으므로 기록은 사라지지 않는다.

## 시트 업무

`완료 내역` 시트에서 작업 결과를 기록하고 완료 알림톡을 보낸다.

1. 장악 키워드 입력
2. 블로그 URL 입력
3. 완료일 입력
4. 발송 드롭박스를 `발송하기`로 변경 → 알림톡 발송

- **세 값이 모두 채워져야** 발송된다. 하나라도 비면 자동으로 `발송대기`로 되돌아간다.
- 발송 후에는 발송시간이 기록돼 **재선택해도 다시 발송되지 않는다.**
- 그 외 열은 보호돼 있다. 입력이 필요하면 토미에게 요청한다.

## 막혔을 때

- 권한 문제(시트 열이 잠김, `clasp` 실패, Vercel 접근 불가) → 토미
- 위 "손대면 안 되는 것"에 해당하는 요청 → 토미
- 프론트 수정이 라이브에 안 보임 → Pages 반영에 수 분 걸린다. 그 후에도 안 보이면 캐시 강제 새로고침(Ctrl+F5)
